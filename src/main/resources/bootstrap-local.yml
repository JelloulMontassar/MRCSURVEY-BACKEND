spring:
  cloud:
    vault:
      uri: https://my-vault-app-f93d58bccb0a.herokuapp.com
      authentication: token
      token: hvs.CAESIH_7Pz78iq5WeFKFPVdKkwznjB2MCpAdiMHRtQQvBgX2Gh4KHGh2cy5iUndjWVROYjh1Z05MYkVoT2xQWlFIVUo
      kv:
        enabled: true
        backend: mysql
        default-context: database
      application-name: database
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  datasource:
    url: jdbc:mysql://${host}:${port}/${database}?useSSL=true&verifyServerCertificate=false
    username: ${username}
    password: ${password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 20000

base:
  url: http://localhost:8089
jwt:
  secret: ${jwt_secret}
