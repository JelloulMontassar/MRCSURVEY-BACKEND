spring:
  application:
    name: database
  cloud:
    vault:
      uri: https://my-vault-app-f93d58bccb0a.herokuapp.com
      authentication: token
      token: hvs.IGW1rYFQjCvFTCh2atUVr7BC
      kv:
        enabled: true
        backend: mysql
        default-context: database
  config:
    import: vault://
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
    beans:
      enabled: true
