spring:
  cloud:
    vault:
      uri: ${SPRING_CLOUD_VAULT_URI}
      authentication: token
      token: ${SPRING_CLOUD_VAULT_TOKEN}
      kv:
        enabled: true
        backend: mysql
        default-context: database
      application-name: database
  datasource:
    url: jdbc:mysql://sql.freedb.tech:3306/freedb_organizecrm?useCompression=true&useSSL=true
    username: freedb_organizecrm
    password: ?e5t8?QNB$aN!DG
    driver-class-name: com.mysql.cj.jdbc.Driver
base:
  url: http://localhost:8089
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
    beans:
      enabled: true
